Currently, we allow the user to add a box to the card with the ```box syntax.
However, it seems within those boxes markdown is disabled.
Can we prevent that? Perhaps we need to move away from the "```" usage and use a different syntax.